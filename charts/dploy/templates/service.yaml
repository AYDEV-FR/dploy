apiVersion: v1
kind: Service
metadata:
  name: {{ include "dploy.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dploy.selectorLabels" . | nindent 4 }}
