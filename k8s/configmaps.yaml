---
# ConfigMap for Dploy API configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: dploy-api-config
  namespace: dploy-system
data:
  ARGOCD_NAMESPACE: "argocd"
  ARGOCD_PROJECT: "dploy"
  MAX_ENVIRONMENTS_PER_USER: "5"
  DEFAULT_TTL: "86400"  # 24 hours in seconds
  EXTEND_TTL: "7200"    # 2 hours in seconds
  BASE_DOMAIN: "env.dploy.localhost"
  SERVER_HOST: "0.0.0.0"
  SERVER_PORT: "8080"
  JWT_AUDIENCE: "dploy"
  JWT_USERNAME_CLAIM: "sub"  # Use "sub" claim for username (standard OIDC)
  OIDC_CLIENT_ID: "dploy"
  OIDC_CLIENT_SECRET: "dploy-secret"
  OIDC_REDIRECT_URL: "http://dploy.localhost/auth/callback"

---
# Secret for Dploy API (OIDC backend configuration)
# Uses OIDC discovery - endpoints are fetched automatically from issuer
apiVersion: v1
kind: Secret
metadata:
  name: dploy-api-secrets
  namespace: dploy-system
type: Opaque
stringData:
  JWKS_URL: "http://authentik-server.authentik.svc.cluster.local/application/o/dploy/jwks/"
  # JWT issuer must match what the OIDC provider puts in the token (internal URL for token exchange)
  JWT_ISSUER: "http://authentik-server.authentik.svc.cluster.local/application/o/dploy/"
  # OIDC issuer URLs - discovery will fetch endpoints from .well-known/openid-configuration
  OIDC_ISSUER: "http://authentik-server.authentik.svc.cluster.local/application/o/dploy"
  OIDC_PUBLIC_ISSUER: "http://auth.dploy.localhost/application/o/dploy"

---
# Available environments
apiVersion: v1
kind: ConfigMap
metadata:
  name: dploy-environments
  namespace: dploy-system
data:
  environments.yaml: |
    environments:
    
      - name: debian-terminal
        description: "Terminal web pour étudiants"
        chart: "github.com/AYDEV-FR/dploy-charts/webshell@main"
        enabled: true
        icon: "terminal"
        ttl: 300
        category: terminal
        # Example with extraValues and variable replacement
        # Supported variables: ${username}, ${user}, ${uuid}, ${ingressHost}, ${ingress}
        extraValues: |
          container:
            image:
              repository: debian
              tag: bookworm
          networkPolicy:
            enabled: false
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []

      - name: ubuntu-terminal
        description: "Terminal web pour étudiants"
        chart: "github.com/AYDEV-FR/dploy-charts/webshell@main"
        enabled: true
        icon: "terminal"
        ttl: 300
        category: terminal
        extraValues: |
          container:
            image:
              repository: ubuntu
              tag: latest
          networkPolicy:
            enabled: false
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []

      - name: webterm-toolbox
        description: "Terminal web pour étudiants"
        chart: "github.com/AYDEV-FR/dploy-charts/webshell@main"
        enabled: true
        icon: "terminal"
        ttl: 300
        category: terminal
        extraValues: |
          container:
            image:
              repository: aydev/toolbox
              tag: latest
          networkPolicy:
            enabled: false
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []

      - name: kali-desktop
        description: "Kali Desktop"
        chart: "github.com/AYDEV-FR/dploy-charts/kasm@main"
        enabled: false
        icon: "box"
        ttl: 300
        category: desktop
        extraValues: |
          image:
            repository: kasmweb/core-kali-rolling
            tag: 1.18.0
          networkPolicy:
            enabled: false
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []

      - name: ubuntu-desktop
        description: "Ubuntu Desktop"
        chart: "github.com/AYDEV-FR/dploy-charts/kasm@main"
        enabled: true
        icon: "box"
        ttl: 300
        category: desktop
        extraValues: |
          image:
            repository: kasmweb/ubuntu-noble-desktop
            tag: 1.18.0
          networkPolicy:
            enabled: false
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []

      - name: chromium
        description: "Remote Virtual Browser"
        chart: "github.com/AYDEV-FR/dploy-charts/kasm@main"
        enabled: true
        icon: "web"
        ttl: 300
        category: desktop
        extraValues: |
          image:
            repository: kasmweb/chromium
            tag: 1.18.0
          networkPolicy:
            enabled: false
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []

      - name: vscode
        description: "VSCode"
        chart: "github.com/AYDEV-FR/dploy-charts/vscode@main"
        enabled: true
        icon: "book"
        ttl: "300,3600"
        extraValues: |
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []

      - name: learning-linux
        description: "learning-linux"
        chart: "github.com/AYDEV-FR/learning-ui/chart@main"
        enabled: true
        icon: "book"
        ttl: "-1"
        category: learning,linux
        extraValues: |
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []

      - name: learning-kubernetes
        description: "learning-kuberentes"
        chart: "github.com/AYDEV-FR/learning-ui/chart@main"
        enabled: true
        icon: "book"
        ttl: "300,3600,2"
        category: learning,kubernetes
        valueFiles:
          - kubernetes01.values.yaml
        extraValues: |
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []

      - name: learning-git
        description: "learning-kuberentes"
        chart: "github.com/AYDEV-FR/learning-ui/chart@main"
        enabled: true
        icon: "book"
        ttl: "300,300,1"
        category: learning,git
        valueFiles:
          - git.values.yaml
        extraValues: |
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: ${ingressHost}
                paths:
                  - path: /
                    pathType: ImplementationSpecific
            tls: []